{
  "name": "glob_files",
  "description": "Matches files in the workspace sandbox against a glob pattern. Supports * (any chars except /) and ** (any path segments).",
  "parameters": {
    "type": "object",
    "properties": {
      "pattern": {
        "type": "string",
        "description": "Glob pattern to match file paths against (e.g. '**/*.ts', 'src/*.json')."
      }
    },
    "required": ["pattern"],
    "additionalProperties": false
  }
}
