{
  "name": "edit_file",
  "description": "Performs a find-and-replace edit on a file in the workspace sandbox. Replaces all occurrences of old_text with new_text.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Relative path to the file within the workspace."
      },
      "old_text": {
        "type": "string",
        "description": "The text to find in the file."
      },
      "new_text": {
        "type": "string",
        "description": "The text to replace old_text with."
      }
    },
    "required": ["path", "old_text", "new_text"],
    "additionalProperties": false
  }
}
