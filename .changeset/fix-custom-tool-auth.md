---
"@amodalai/runtime": patch
"@amodalai/core": patch
---

Fix custom tool ctx.request() to use correct connection config field names (base_url, \_request_config.auth) and enforce write intent for mutating HTTP methods
