---
"@amodalai/core": patch
"@amodalai/runtime": patch
---

Upgrade logger with child() scoped loggers, JSON output mode, and sanitize hook. Backward-compatible with existing log.info(message, tag) call pattern.
