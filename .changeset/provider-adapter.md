---
"@amodalai/runtime": patch
---

Add Vercel AI SDK v6 dependencies (ai, @ai-sdk/anthropic, @ai-sdk/openai, @ai-sdk/google) and LLMProvider abstraction with createProvider() factory. Includes content generator bridge specification for Phase 1.3.
