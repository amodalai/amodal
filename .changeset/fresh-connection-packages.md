---
"@amodalai/core": patch
---

Load agent packages from the Amodal package install directory, merge packaged connection specs with local connection overrides, and honor the documented `api_key` auth type.
